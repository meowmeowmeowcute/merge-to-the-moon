# RETROSPECTIVE

> 完整的 AI 出錯紀錄在 [docs/ai-incidents.md](docs/ai-incidents.md)（共 7 則），這裡挑重點回答。

## 1. 用了哪些 AI / Agent？

**Claude Code**（Claude 桌面版的 Code 分頁，模型 Claude Opus 5.5）。從整理目標文件、寫 Spec 和測試、實作，到在內建瀏覽器驗證畫面、用 `gh` 建 repo 和部署到 GitHub Pages，都是同一個 Agent 在做。

## 2. AI 做得最好的一件事？

**把 Spec 寫到可以直接變成測試的程度，再照 TDD 一步一步推進。** SPEC 裡把合成規則寫成具體的行為，例如「同一物件在一個 substep 內最多合成一次」、「合成位置取中點再 clamp」，P2 就能直接寫出 61 個會失敗的測試。之後 P3～P5 每一階段都是「讓某一組測試轉綠」，所以進度很清楚。最擔心的「密集堆疊中合成不會把東西彈飛」測試，實作一次就通過。

## 3. AI 搞砸的一件事？

**寫了一個物理上不會發生的測試情境。**（Incident #4，下面詳述）

其他還有：`npm test` 的指令在 Node 24 不能用（#1）、用 PowerShell 改檔把中文弄成亂碼（#2）、主迴圈的時間差算錯（#5）。

## 4. 你怎麼發現它錯了？

靠**測試的 expected 和 actual**，以及**讓 AI 印出物理軌跡**。P4 實作完合成後只剩一個測試失敗：`expected [4]`，`actual [3, 3]`，兩顆物件沒有合成。追蹤每 15 步的位置後發現，兩顆在相距 18px 的地方就停下來了。

## 5. 最後怎麼解決？

先分清楚是「實作錯」還是「測試假設錯」。軌跡顯示合成邏輯沒問題，是摩擦力加空氣阻力讓物件停下來，所以**改的是測試情境**（起點拉近、初速加大），不是去改實作遷就測試。並且在 incident 紀錄裡寫下規則：物理類測試要先確認情境真的會發生，再寫斷言。

## 6. 重做一次，你會怎麼改變協作方式？

- **每個設定檔寫完當下就實際跑一次。** #1 就是因為 P1 時還沒裝 Node，指令沒驗證就交出去了。
- **TDD 的 Red 階段要看 pass 數。** 不應該通過卻通過的測試就是寫錯的測試（#3）。
- **瀏覽器端的膠水程式要更早想好怎麼驗證。** `main.js` 沒有自動測試，#5 是靠 review diff 才抓到；真實滑鼠點擊也直到部署後才第一次實測（#6）。下次會把更多邏輯放進可測試的模組，並從一開始就用真實輸入測試。
- **更早試玩。** 「拿掉下一個預覽」、「合成要彈一下」、「難度太低」都是實際玩過才發現的。如果 P6 一做出可玩的 demo 就試玩，Spec 可以更早修正。
- **測試不要寫死可調的參數。** 調難度時才發現有幾個測試直接寫了舊半徑（#7）。位置和門檻應該從 config 算出來，調參數時測試才不會跟著壞。
- **數值平衡要靠玩，不是靠猜。** 下次會先做一個 `?debug` 用的調參面板，在遊戲裡即時調整、找到手感後再寫回 Spec。

---

## 試玩後的調整：提高難度

部署後實際玩了幾局，覺得**難度太低**：警戒線很高、物件相對容器偏小，很難輸。這次的修改完整走了一輪 SDD → TDD。

1. **先問可以調哪裡，不直接改。** 請 AI 列出影響難度的地方。它把難度拆成兩個壓力：「容器空間多快被填滿」和「掉下來的東西有多難配對」，並列出 7 個可調的數值（警戒線、半徑、生成機率、超線時間、彈性等），以及 3 種機制改法，最後給了建議組合。
2. **我做決定。** 選了建議的三項數值調整：警戒線 120→170、物件放大 15%、生成改成 1～5 階。另外加上「難度隨分數上升」：200／500／1000 分時升級，生成的物件越來越大、超線容許時間從 2.0 秒縮到 1.2 秒。
3. **先改 Spec（v1.4）。** 新增 `LEVELS` 難度表，並把它的規則寫清楚：門檻遞增、容許時間不遞增、平均生成階數不遞減、一次跳多級只發一次升級事件。這些規則後來都直接變成了測試。
4. **紅燈 → 綠燈。** 先寫 12 個新測試並確認失敗，再實作，最後 90 個測試全部通過。物件放大後，最擔心的「堆疊不重疊」物理測試也沒有壞。
5. **畫面和上線。** HUD 顯示等級、升級橫幅和音效；再推到 GitHub，CI 通過後自動部署。共 4 個 commit，看得出「Spec → 測試 → 邏輯 → 畫面」的順序。

**過程中發現的問題**
- **測試寫死舊半徑**（#7）：合成測試裡有 `676`（地板高度減舊半徑）、`32`（舊的第 4 階半徑）這種數字，物件一放大就會失效。改寫測試時先檢查到，全部改成用 `radiusOf()` 計算。
- **統計測試分辨力不夠**（#7）：「升級後生成機率變大」原本只抽 400 次、門檻 9%，沒升級（8%）時也可能誤判通過。自我檢查時發現，改成抽 1000 次、門檻 10.5%。
- **分享預覽圖跑版**：物件放大後，預覽圖裡的月亮蓋到了標題文字，看過圖片後調整位置並重新產生。

**這次學到的**：Spec 和測試讓「改遊戲規則」變得很安心，改完馬上知道有沒有改壞東西。但測試本身也要跟著 Spec 設計，數值要從 config 讀，不能寫死；統計類的測試也要算過能不能分辨兩種情況。

---

## Incident：「從兩側滑向彼此會合成」的測試情境不成立（P4）

- **我想要什麼**：驗證兩個同階物件在地面上互相靠近時會合成（SPEC AC4）。
- **AI 做了什麼**：P2 寫的測試讓兩個第 3 階物件從 x=120 和 x=280 以初速 ±3 相向滑動，預期 180 步內合成。P4 實作合成邏輯後，其他 14 個合成測試都通過，只有這個失敗。
- **我怎麼發現**：測試輸出 `expected [4]`，`actual [3, 3]`。讓 AI 寫一個小腳本印出每 15 步的位置和速度後發現，地面摩擦加上空氣阻力，15 步內就把速度從 3 降到 0.75，兩顆最後停在 x≈167 和 233，還差 18px 才碰到。
- **怎麼解決**：確認合成邏輯正確、是測試的物理假設錯誤後，把情境改成從 x=150、250 以 ±6 出發，測試通過。**流程上的修正**：物理情境的測試先用腳本印出軌跡，確認情境真的會發生再寫斷言；測試失敗時先判斷是實作錯還是測試錯，不直接改實作去遷就測試。
